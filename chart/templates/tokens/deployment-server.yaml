{{- if .Values.tokens.server.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.tokens.server.name }}
  labels:
    app: {{ .Values.tokens.server.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ .Values.tokens.server.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.tokens.server.name }}
        appVersion: {{ .Chart.AppVersion }}
        helmVersion: {{ .Chart.Version }}
    spec:
      serviceAccountName: {{ .Values.tokens.serviceAccount.name }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: tokens-spc
      containers:
        - name: {{ .Values.tokens.server.name }}
          image: "{{ .Values.tokens.image.repository }}:{{ .Values.tokens.image.tag }}"
          imagePullPolicy: {{ .Values.tokens.image.pullPolicy }}
          command: ['yarn', 'server:start']
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
          ports:
            - name: www
              containerPort: {{ .Values.tokens.server.service.www.port }}
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: {{ .Values.tokens.server.service.www.port }}
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: {{ .Values.tokens.server.service.www.port }}
            timeoutSeconds: 5
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-{{ .Values.tokens.server.name }}-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: tokens-secrets
                  key: db_username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: tokens-secrets
                  key: db_password
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: tokens-secrets
                  key: db_hostname
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: tokens-secrets
                  key: db_port
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: tokens-secrets
                  key: db_name
{{- end }}
