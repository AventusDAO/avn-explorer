{{- if or .Values.tokens.server.enabled .Values.tokens.processor.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.tokens.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.awsAccountId }}:role/{{ .Values.tokens.serviceAccount.role }}@{{ .Release.Namespace }}
{{- end }}
