{{- if .Values.balances.server.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.balances.server.name }}
  labels:
    app: {{ .Values.balances.server.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ .Values.balances.server.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.balances.server.name }}
        appVersion: {{ .Chart.AppVersion }}
        helmVersion: {{ .Chart.Version }}
    spec:
      serviceAccountName: {{ .Values.balances.serviceAccount.name }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: balances-spc
      containers:
        - name: {{ .Values.balances.server.name }}
          image: "{{ .Values.balances.image.repository }}:{{ .Values.balances.image.tag }}"
          imagePullPolicy: {{ .Values.balances.image.pullPolicy }}
          command: ['yarn', 'server:start']
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
          ports:
            - name: www
              containerPort: {{ .Values.balances.server.service.www.port }}
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: {{ .Values.balances.server.service.www.port }}
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: {{ .Values.balances.server.service.www.port }}
            timeoutSeconds: 5
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-{{ .Values.balances.server.name }}-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: balances-secrets
                  key: db_username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: balances-secrets
                  key: db_password
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: balances-secrets
                  key: db_hostname
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: balances-secrets
                  key: db_port
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: balances-secrets
                  key: db_name
{{- end }}
