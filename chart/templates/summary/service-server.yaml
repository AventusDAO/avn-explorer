{{- if .Values.summary.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.summary.server.name }}
  labels:
    app: {{ .Values.summary.server.name }}
    helmVersion: {{ .Chart.Version }}
spec:
  type: {{ .Values.summary.server.service.www.type }}
  ports:
  - name: {{ .Values.summary.server.name }}
    port: {{ .Values.summary.server.service.www.port }}
    targetPort: {{ .Values.summary.server.service.www.port }}
    protocol: TCP
  selector:
    app: {{ .Values.summary.server.name }}
{{- end }}
