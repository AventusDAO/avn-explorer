{{- if .Values.summary.server.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.summary.server.name }}
  labels:
    app: {{ .Values.summary.server.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ .Values.summary.server.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.summary.server.name }}
        appVersion: {{ .Chart.AppVersion }}
        helmVersion: {{ .Chart.Version }}
    spec:
      serviceAccountName: {{ .Values.summary.serviceAccount.name }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: summary-spc
      containers:
        - name: {{ .Values.summary.server.name }}
          image: "{{ .Values.summary.image.repository }}:{{ .Values.summary.image.tag }}"
          imagePullPolicy: {{ .Values.summary.image.pullPolicy }}
          command: ['yarn', 'server:start']
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
          ports:
            - name: www
              containerPort: {{ .Values.summary.server.service.www.port }}
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: {{ .Values.summary.server.service.www.port }}
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: {{ .Values.summary.server.service.www.port }}
            timeoutSeconds: 5
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-{{ .Values.summary.server.name }}-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: summary-secrets
                  key: db_username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: summary-secrets
                  key: db_password
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: summary-secrets
                  key: db_hostname
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: summary-secrets
                  key: db_port
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: summary-secrets
                  key: db_name
{{- end }}
