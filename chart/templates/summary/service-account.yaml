{{- if or .Values.summary.server.enabled .Values.summary.processor.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.summary.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.awsAccountId }}:role/{{ .Values.summary.serviceAccount.role }}@{{ .Release.Namespace }}
{{- end }}
