{{- if or .Values.staking.server.enabled .Values.staking.processor.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.staking.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.awsAccountId }}:role/{{ .Values.staking.serviceAccount.role }}@{{ .Release.Namespace }}
{{- end }}
