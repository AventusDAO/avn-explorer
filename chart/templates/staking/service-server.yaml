{{- if .Values.staking.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.staking.server.name }}
  labels:
    app: {{ .Values.staking.server.name }}
    helmVersion: {{ .Chart.Version }}
spec:
  type: {{ .Values.staking.server.service.www.type }}
  ports:
  - name: {{ .Values.staking.server.name }}
    port: {{ .Values.staking.server.service.www.port }}
    targetPort: {{ .Values.staking.server.service.www.port }}
    protocol: TCP
  selector:
    app: {{ .Values.staking.server.name }}
{{- end }}
