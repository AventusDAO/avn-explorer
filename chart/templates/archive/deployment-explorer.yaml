{{- if .Values.archive.explorer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.archive.explorer.name }}
  labels:
    app: {{ .Values.archive.explorer.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ .Values.archive.explorer.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.archive.explorer.name }}
        appVersion: {{ .Chart.AppVersion }}
        helmVersion: {{ .Chart.Version }}
    spec:
      serviceAccountName: {{ .Values.archive.serviceAccountName }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: archive-spc
      containers:
        - name: {{ .Values.archive.explorer.name }}
          image: "{{ .Values.archive.explorer.image.repository }}:{{ .Values.archive.explorer.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.archive.explorer.image.pullPolicy }}
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
          ports:
            - name: www
              containerPort: {{ .Values.archive.explorer.service.www.port }}
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: {{ .Values.archive.explorer.service.www.port }}
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: {{ .Values.archive.explorer.service.www.port }}
            timeoutSeconds: 5
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: archive-secrets
                  key: db_username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: archive-secrets
                  key: db_password
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: archive-secrets
                  key: db_hostname
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: archive-secrets
                  key: db_port
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: archive-secrets
                  key: db_name
            - name: DB_TYPE
              valueFrom:
                secretKeyRef:
                  name: archive-secrets
                  key: db_type
{{- end }}
