type Account @entity {
  id: ID!
  stakedAmountTotal: BigInt!
  rewardsTotal: BigInt!
  updatedAt: Int
}

type ChainState @entity {
  id: ID!
  timestamp: DateTime! @index
  blockNumber: Int! @index
}
