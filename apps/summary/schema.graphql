type SummaryRoot @entity {
  id: ID!
  rootHash: String
  fromBlock: Int
  toBlock: Int!
  isValidated: Boolean!
}

type ChainState @entity {
  id: ID!
  timestamp: DateTime! @index
  blockNumber: Int! @index
}
