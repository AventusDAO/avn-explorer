type CalculatedRewards {
  reward1: BigInt!
  reward2: BigInt!
}

type SolutionGroup @entity {
  id: ID!
  votingReward: BigInt
  subscriptionReward: BigInt
  unclaimedRewards: BigInt
  remainingBlocks: Int
  reservedFunds: BigInt
  calculatedRewards: CalculatedRewards
}
