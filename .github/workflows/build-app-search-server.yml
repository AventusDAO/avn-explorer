name: Build Search Server

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
    paths:
      - 'apps/search-server/**'
      - 'packages/types/**'
      - 'packages/config/**'
      - 'packages/utils/**'
  push:
    branches:
      - main
    paths:
      - 'apps/search-server/**'
      - 'packages/types/**'
      - 'packages/config/**'
      - 'packages/utils/**'

jobs:
  build:
    name: AvN Explorer Search Server
    uses: ./.github/workflows/docker-build.yml
    secrets: inherit
    with:
      short_image_name: search-server
      ecr_repository_prefix: avn-explorer
      aws_region: eu-west-2
      dockerfile: apps/search-server/Dockerfile
