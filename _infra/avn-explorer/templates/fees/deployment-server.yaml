{{- if .Values.fees.server.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.fees.server.name }}
  labels:
    app: {{ .Values.fees.server.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ .Values.fees.server.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.fees.server.name }}
        appVersion: {{ .Chart.AppVersion }}
        helmVersion: {{ .Chart.Version }}
    spec:
      serviceAccountName: {{ .Values.fees.serviceAccount.name }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: fees-spc
      containers:
        - name: {{ .Values.fees.server.name }}
          image: "{{ .Values.fees.image.repository }}:{{ .Values.fees.image.tag }}"
          imagePullPolicy: {{ .Values.fees.image.pullPolicy }}
          command: ['yarn', 'server:start']
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
          ports:
            - name: www
              containerPort: {{ .Values.fees.server.service.www.port }}
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: {{ .Values.fees.server.service.www.port }}
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: {{ .Values.fees.server.service.www.port }}
            timeoutSeconds: 5
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-{{ .Values.fees.server.name }}-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: fees-secrets
                  key: db_username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: fees-secrets
                  key: db_password
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: fees-secrets
                  key: db_hostname
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: fees-secrets
                  key: db_port
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: fees-secrets
                  key: db_name
{{- end }}
